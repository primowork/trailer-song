# מוצמד ולא >= : טווח פתוח אומר שכל בנייה מחדש של תמונת ה-Docker יכולה
# למשוך גרסת Streamlit חדשה יותר בלי שאף אחד ביקש את זה — כולל שינויים
# פנימיים באיך שריצת סקריפט שהופרעה (rerun שני לפני שהראשון הסתיים)
# מתיישבת מול הדפדפן. זה בדיוק התחום שבו נחקר באג של פקדים שנשארים
# כפולים על המסך אחרי הגרלות מהירות ברצף — ובלי גרסה קבועה, "עובד אצלי"
# לא אומר הרבה כשסביבת הפיתוח וסביבת הריצה בפועל יכולות להיבנות בגרסאות
# שונות של הספרייה.
streamlit==1.62.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
thefuzz>=0.22.0
# מדידת עוצמת האודיו רצה בדפדפן של המשתמש (Web Audio API) ולכן אין כאן תלות
# ניתוח אודיו: אין numba/llvmlite, אין מאות MB בתמונת ה-Docker.
# Postgres, לבקאנד השרתי של `storage.py`. אופציונלי בפועל: בלי
# `DATABASE_URL` הקוד לא נוגע בו כלל וממשיך על קבצי JSON.
psycopg[binary]>=3.1
